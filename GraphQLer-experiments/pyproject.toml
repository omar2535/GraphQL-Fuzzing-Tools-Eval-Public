[project]
name = "graphqler-experiments"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "graphqler",
]

[tool.uv.sources]
graphqler = { git = "https://github.com/omar2535/GraphQLer" }
